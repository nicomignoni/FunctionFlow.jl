var documenterSearchIndex = {"docs":
[{"location":"#FunctionFlow.jl","page":"Home","title":"FunctionFlow.jl","text":"Automatic lazy dependency resolution for computational DAGs in Julia.","category":"section"},{"location":"#Installation","page":"Home","title":"Installation","text":"Install FunctionFlow.jl from the Julia REPL\n\nadd ] https://github.com/nicomignoni/FunctionFlow.jl.git","category":"section"},{"location":"#Quickstart","page":"Home","title":"Quickstart","text":"Suppose you wrote a mathematical model characterized by the following functions\n\nf(a) = 2a + 3\ng(a, b) = b^2 - a\nh(g, b) = g + 4b\nk(f, h) = h * (f - 1)\n\nSome function arguments are themselves functions; this dependency can be expressed through a directed acycilc graph (DAG) as follows\n\n<img src=\"assets/dag.svg\" width=\"30%\">\n\nIn order to calculate k, you need to compute f, g, and h first, which can be tedious and error-prone when the computational DAG grows. Since all these functions can be eventually expressed with respect to a and b, it would be more convenient to be able to write f(a, b) (or h(a, b), for example). \n\nFunctionFlow.jl does exactly this: it allows you to write a function as a Node of a computational DAG, which can be in turn evaluated as a function of the base arguments only.\n\nusing FunctionFlow\n\nf_node = Node(f, :a)\ng_node = Node(g, :a, :b)\nh_node = Node(h, g_node, :b)\nk_node = Node(k, f_node, h_node)\n\nnothing # hide\n\nA Node is constructed by passing the function it represents, and the \"arguments\", either Symbols for base arguments or other Nodes. A Node automatically shows its dependency on base arguments\n\nk_node\n\nWe can now pass a and b as keyword arguments to k_node to compute k without explicitly evaluating h, g, and f beforehand \n\nk_node(a = 3, b = 2)\n\nBy recursively substituting h, g, and f in k, we can rewrite it as explicitly dependent on a and b\n\nk_explicit(a, b) = (b^2 - a + 4b) * (2a + 2)\n\nnothing # hide\n\nand then check that it would produce the same result\n\nk_explicit(3, 2) == k_node(a = 3, b = 2)\n\nEach Node caches its value, based on the hashed base, in a Least-Recently-Used cache provided by LRUCache.jl.\n\nusing LRUCache\n\ncache_info(k_node.cache)\n\nDefault cache size is 1, but it can be incremented by passing cache_size to the Node constructor. ","category":"section"}]
}
